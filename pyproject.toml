[project]
name = "awesome-jev-security"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Vlad", email = "" }
]
requires-python = ">=3.14"
dependencies = [
    "datasets>=5.0.1",
]

[project.scripts]
awesome-jev-security = "awesome_jev_security:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
